---

- name: Debian | Ensure required packages are installed
  apt:
    name: '{{ mount_packages["Debian"][item.fstype] }}'
    state: 'latest'
  when: (item.fstype is string and item.fstype in ['cifs', 'nfs'])
  with_items: mount_list_combined
