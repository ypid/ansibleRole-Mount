---

# "Global" mounts
mount_list:
  # - src: 'tmpfs'
  #   dest: '/tmp'
  #   fstype: 'tmpfs'
  #   opts: 'nodev,nosuid,size=50%'

# "Host group" mounts
mount_group_list:

# "Host" mounts
mount_host_list:


mount_packages:
  Debian:
    nfs: 'nfs-common'
    cifs: 'cifs-utils'
